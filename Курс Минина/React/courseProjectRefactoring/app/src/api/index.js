import * as users from "./fake.api/user.api";
// импортируем ВСЕ из users как users
import professions from "./fake.api/professions.api";

const API = {
  users,
  professions,
};
export default API;
